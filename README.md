GossipSub with go-libp2p pubsub and kadmelia dht discovery.

option 1: bootstrap node with empty discovery nodes

option 2: node with bootstrap nodes as discovery nodes

optional setup local mdns discovery